{
    "buildf": [{
        "src": "config/wsgi.py",
        "use": "vercel/python",
        "config": { "msxLambdaSize": "15mb", "runtime": "Python 3.12.3"}
    }],
    "routes": [
        {
            "src":"/(.*)",
            "dest": "config/wsgi.py"
        }
    ]
}